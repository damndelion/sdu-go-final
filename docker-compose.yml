version: '3.8'
services:

  postgres:
    image: postgres:latest
    environment:
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"

  app:
    build: .
    container_name: app
    image: app
    environment:
      PG_URL: 'postgres://postgres:password@postgres/postgres'
    ports:
      - 8080:8080
    depends_on:
      - postgres

volumes:
  pg-data: